## Packages
framer-motion | Smooth animations for route transitions and micro-interactions
date-fns | Formatting timestamps (e.g., "2 hours ago")

## Notes
- Audio recording uses browser native MediaRecorder API
- Audio uploads go to Replit Object Storage via presigned URLs
- Authentication is handled by Replit Auth (OpenID Connect)
